import 'package:flutter/material.dart';

class ChangeProfilePopup extends StatelessWidget {
  const ChangeProfilePopup({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
